# Plant Disease Detector 🌿

CNN-based plant leaf disease classification system trained on the PlantVillage dataset.

## Tech Stack
- TensorFlow / Keras
- Streamlit
- Python

## Prerequisites
To run this project locally, ensure the following are installed:
- Python 3.10.x
- pip
- Git (optional, for cloning)

⚠️ TensorFlow does not currently support Python 3.12+.
Python 3.10.x is recommended for compatibility.

## Installation
```bash
git clone https://github.com/jaivarun47/plant-disease-detector.git
cd plant-disease-detector
python -m venv venv
venv\Scripts\activate   # Windows
# source venv/bin/activate  # Linux / macOS
pip install -r requirements.txt
```

python -m venv venv
venv\Scripts\activate # Windows # source venv/bin/activate  # Linux / macOS  

## How to Run
```bash
pip install -r requirements.txt
cd app
streamlit run app.py
```
## Dataset source
The data set used to train the model in this demo was PlantVillage dataset which can be obtained from this url:
https://www.kaggle.com/datasets/emmarex/plantdisease



